#include "protocol_core_harness.hpp"

#include <iostream>
#include <string>

int main()
{
    std::string failure;
    if (!aida::standalone::tests::mcp_compat::run_protocol_core_harness(failure)) {
        std::cerr << failure << '\n';
        return 1;
    }
    std::cout << "protocol_core_harness source contract satisfied\n";
    return 0;
}
